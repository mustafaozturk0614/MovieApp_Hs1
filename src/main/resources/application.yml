server:
  port: 8090

spring:
  datasource:
    url: jdbc:postgresql://hs1postgres/moviedb
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

